<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Routes</title>
    <link rel='stylesheet' href='/styles/bootstrap.min.css' />
    <link rel='stylesheet' href='/styles/style.css' />
    <script src="/js/jquery-2.2.0.min.js" type="text/javascript"></script>
    <script src="/js/bootstrap.min.js" type="text/javascript"></script>
    <title>Document</title>
</head>

<body>
    <script>
        $(document).ready(function () {
            $("#CreateButton").click(function () {
                const routename = $('#routename').val();

                if (!routename) {
                    alert('please enter route name ')
                    return
                }
                const data = {
                    routename
                };
                $.ajax({
                    type: "PUT",
                    url: "/api/v1/route/" + routeId,
                    data,
                    success: function (serverResponse) {
                        if (serverResponse) {
                            alert('Successfully updated');
                            location.href = '/manage/routes';
                        }
                    },
                    error: function (errorResponse) {
                        if (errorResponse) {
                            alert(`Error creatring station: ${errorResponse.responseText}`);
                        }
                    }
                });
            });
        });
    </script>


    <div>
        <nav class="navbar navbar-inverse" style="padding-left:130px;">
            <ul class="nav navbar-nav">
                <li class="active"><a href="/">Home</a></li>
            </ul>
        </nav>   
    </div>
    <div class="center">

        <h1>Update Route: <span class="text-danger">{{route.routename}}</span></h1>

    </div>
    <div class="input-group ">
        <div style="padding-left: 500px">

            <form class="form-horizontal" style="width: 50%;">

                <div class="form-group">
                    <input id="routename" value="{{route.routename}}" type="text" class="form-control"
                        placeholder="Route Name">
                
                    <div class="col-sm-10 col-sm-offset-2">
                        <input id="CreateButton" name="Update" type="button" value="update" class="btn btn-primary">
                    </div>
                </div>
            </form>
        </div>
    </div>
</body>

</html>